#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first number
 * @b: second number
 * Return: the result
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);

}
